services:
  postgres:
    image: postgres:16
    container_name: postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: twn
      POSTGRES_PASSWORD: twn
      POSTGRES_DB: twn
    volumes:
      - pgdata-prod:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U twn -d twn"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - pg

  twn:
    image: ghcr.io/infybtw/twn
    build:
      context: .
      dockerfile: app/Dockerfile
      network: host
    env_file: .env
    restart: unless-stopped
    networks:
      - pg
    volumes:
      - ./logs/prod:/app/logs
    depends_on:
      postgres:
        condition: service_healthy

  adminer:
    image: adminer
    restart: unless-stopped
    ports:
      - "8080:8080"
    networks:
      - pg

  pgbackweb:
    image: eduardolat/pgbackweb:latest
    ports:
      - "8085:8085"
    volumes:
      - ./backups:/backups
    environment:
      PBW_ENCRYPTION_KEY: "secret_key"
      PBW_POSTGRES_CONN_STRING: "postgresql://twn:twn@postgres:5432/pgbackweb?sslmode=disable"
      PBW_PATH_PREFIX: "/pgback"
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - pg

volumes:
  pgdata-prod:

networks:
  pg:
    driver: bridge
