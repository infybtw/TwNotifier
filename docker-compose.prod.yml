services:
  postgres:
    image: postgres:16
    container_name: postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: twn
      POSTGRES_PASSWORD: pass
      POSTGRES_DB: twn
    volumes:
      - pgdata-prod:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U twn -d twn"]
      interval: 5s
      timeout: 5s
      retries: 5
    networks:
      - pg

  twn:
    image: ghcr.io/infybtw/twnotifier
    build:
      context: .
      dockerfile: app/Dockerfile
      network: host
    env_file: .env
    restart: unless-stopped
    networks:
      - pg
    volumes:
      - ./logs/prod:/app/logs
    ports:
      - "127.0.0.1:3000:3000"
    depends_on:
      postgres:
        condition: service_healthy

  web:
    image: ghcr.io/infybtw/twnotifier-web
    build:
      context: .
      dockerfile: Dockerfile-web
    restart: unless-stopped
    environment:
      WEB_DOMAIN: ${WEB_DOMAIN:-localhost}
      KICK_WEBHOOK_PATH: ${KICK_WEBHOOK_PATH}
      TWITCH_WEBHOOK_PATH: ${TWITCH_WEBHOOK_PATH}
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - caddy-data:/data
      - caddy-config:/config
    depends_on:
      - twn
    networks:
      - pg

  adminer:
    image: adminer
    restart: unless-stopped
    ports:
      - "127.0.0.1:8080:8080"
    networks:
      - pg

  pgbackweb:
    image: eduardolat/pgbackweb:latest
    restart: unless-stopped
    ports:
      - "127.0.0.1:8085:8085"
    environment:
      PBW_ENCRYPTION_KEY: "key"
      PBW_POSTGRES_CONN_STRING: "postgresql://user:pass@postgres:5432/db?sslmode=disable"
      PBW_PATH_PREFIX: "/pgback"
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - pg

volumes:
  pgdata-prod:
  caddy-data:
  caddy-config:

networks:
  pg:
    driver: bridge
